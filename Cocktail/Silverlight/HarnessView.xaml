<UserControl x:Class="Cocktail.HarnessView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk"
             d:DesignHeight="406"
             d:DesignWidth="730"
             mc:Ignorable="d">

    <Grid x:Name="LayoutRoot" Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.2*" />
            <ColumnDefinition Width="0.8*" />
        </Grid.ColumnDefinitions>
        <ItemsControl x:Name="Names" IsEnabled="{Binding Path=Ready}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button cal:Message.Attach="ActivateViewModel($dataContext)" Content="{Binding .}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <sdk:Frame x:Name="ActiveItem" Grid.Column="1" />
    </Grid>
</UserControl>