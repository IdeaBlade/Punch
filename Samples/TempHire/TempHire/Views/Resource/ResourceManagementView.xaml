<UserControl x:Class="TempHire.Views.Resource.ResourceManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk"
             d:DataContext="{Binding Source={StaticResource ViewModelLocator},
                                     Path=ResourceManagementViewModel}"
             d:DesignHeight="400"
             d:DesignWidth="800"
             mc:Ignorable="d">

    <Grid x:Name="LayoutRoot" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="360" MinWidth="360" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <sdk:GridSplitter Width="5" ShowsPreview="True" />
        <sdk:Frame x:Name="SearchPane" Margin="0,0,5,0" />
        <sdk:Frame x:Name="ActiveItem" Grid.Column="1" />
    </Grid>
</UserControl>
